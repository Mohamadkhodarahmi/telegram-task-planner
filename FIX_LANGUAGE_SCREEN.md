# رفع مشکل: صفحه زبان بالا می‌ماند

## مشکل:
وقتی زبان انتخاب می‌شد، صفحه انتخاب زبان hide نمی‌شد و در همه صفحات نمایش داده می‌شد.

## راه حل:

### تغییرات اعمال شده:

1. **تابع showScreen:**
   - اضافه کردن `display: none` به صورت explicit برای همه صفحات
   - اطمینان از اینکه صفحه قبلی کاملاً hide می‌شود

2. **CSS:**
   - استفاده از `!important` برای اطمینان از override شدن
   - صفحه زبان وقتی active است `display: flex` می‌شود
   - وقتی active نیست `display: none` می‌شود

### کد تغییر یافته:

```javascript
// Hide all screens first - use display: none to ensure they're hidden
document.querySelectorAll('.screen').forEach(screen => {
    screen.classList.remove('active');
    screen.style.display = 'none';  // Explicit hide
});
```

```css
.screen.active {
    display: block !important;
    z-index: 1;
}

#language-screen.active {
    display: flex !important;
}
```

## نتیجه:

✅ صفحه زبان وقتی hide می‌شود کاملاً ناپدید می‌شود
✅ صفحات دیگر به درستی نمایش داده می‌شوند
✅ هیچ overlay یا باقی‌مانده‌ای از صفحه زبان نیست

## تست کنید:

1. صفحه را refresh کنید
2. زبان را انتخاب کنید
3. صفحه زبان باید کاملاً ناپدید شود
4. صفحه اصلی باید به صورت کامل نمایش داده شود

## Deploy:

✅ Pages: Deploy شد
✅ Worker: به‌روزرسانی شد

**حالا باید کار کند!** 🎉




