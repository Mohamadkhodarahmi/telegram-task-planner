# 🧪 راهنمای تست Study Planner

## ✅ چک‌لیست تست

### 1. تست اصلی (Basic Flow)
- [ ] باز کردن Mini App از Telegram
- [ ] انتخاب زبان (فارسی/انگلیسی/روسی)
- [ ] مشاهده صفحه اصلی
- [ ] ایجاد برنامه مطالعه جدید
- [ ] مشاهده برنامه ایجاد شده

### 2. تست Today Dashboard (Phase 5)
- [ ] نمایش Today Dashboard در بالای صفحه view-plan
- [ ] نمایش تعداد کارهای باقی‌مانده
- [ ] نمایش درصد تکمیل
- [ ] نمایش Progress bar
- [ ] کلیک روی "Start Study Session" (باید به امروز scroll کند)
- [ ] نمایش پیام تبریک وقتی همه کارها انجام شود

### 3. تست Task Management (Phase 3)
- [ ] اضافه کردن کار جدید (از input)
- [ ] تیک زدن یک کار (باید persist شود)
- [ ] حذف یک کار
- [ ] بستن و باز کردن دوباره app (باید tasks باقی بمانند)

### 4. تست Empty States (Phase 5)
- [ ] باز کردن view-plan بدون برنامه → باید empty state با CTA نمایش داده شود
- [ ] باز کردن progress بدون داده → باید empty state نمایش داده شود

### 5. تست Progress Chart (Phase 5)
- [ ] نمایش چارت با labels و axes
- [ ] نمایش مقادیر روی هر bar
- [ ] نمایش grid lines

### 6. تست Translations (Phase 6)
- [ ] تغییر زبان به فارسی → همه متن‌ها باید فارسی شوند
- [ ] تغییر زبان به انگلیسی → همه متن‌ها باید انگلیسی شوند
- [ ] تغییر زبان به روسی → همه متن‌ها باید روسی شوند

## 🔍 نکات مهم برای تست

1. **Cache**: اگر تغییرات را نمی‌بینید:
   - Mini App را کاملاً ببندید
   - دوباره باز کنید
   - یا در مرورگر: Ctrl+Shift+R (hard refresh)

2. **Console**: برای بررسی خطاها:
   - F12 را بزنید
   - Console tab را ببینید
   - خطاهای قرمز را بررسی کنید

3. **Network**: برای بررسی API calls:
   - F12 → Network tab
   - Request های `/api/user/...` را بررسی کنید
   - Response ها را ببینید

## 🐛 مشکلات احتمالی و راه‌حل

### مشکل: Today Dashboard نمایش داده نمی‌شود
- بررسی کنید که برنامه مطالعه وجود دارد
- Console را برای خطاها بررسی کنید
- مطمئن شوید که `displayStudyPlan` صدا زده می‌شود

### مشکل: Tasks persist نمی‌شوند
- بررسی کنید که userId موجود است
- Network tab را ببینید - آیا API calls موفق هستند؟
- Backend logs را بررسی کنید

### مشکل: startStudySession کار نمی‌کند
- بررسی کنید که day number درست محاسبه می‌شود
- Console را برای خطاها بررسی کنید
- مطمئن شوید که کارت امروز وجود دارد

## 📝 گزارش تست

لطفاً این موارد را تست کنید و نتیجه را گزارش دهید:
- ✅ کدام موارد کار می‌کند
- ❌ کدام موارد مشکل دارند
- 🔍 خطاهای Console (اگر وجود دارد)


