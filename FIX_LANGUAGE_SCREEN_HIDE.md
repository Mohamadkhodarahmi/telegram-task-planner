# رفع مشکل: صفحه زبان بعد از انتخاب زبان نمایش داده می‌شود

## مشکل:
بعد از انتخاب زبان، صفحه زبان hide نمی‌شد و کاربر باید به پایین اسکرول می‌کرد تا به داشبورد برسد.

## راه حل:

### تغییرات اعمال شده:

1. **تابع selectLanguage:**
   - Force hide کردن صفحه زبان قبل از نمایش صفحه اصلی
   - استفاده از `display: none` و `visibility: hidden`
   - تنظیم `z-index: 0` برای اطمینان از اینکه زیر صفحه اصلی است
   - اضافه کردن setTimeout برای اطمینان از hide شدن

2. **تابع showScreen:**
   - اضافه کردن `visibility: hidden` برای همه صفحات
   - تنظیم `z-index` به صورت explicit
   - اطمینان از اینکه صفحه قبلی کاملاً hide می‌شود

3. **CSS:**
   - اضافه کردن rule برای `#language-screen:not(.active)`
   - استفاده از `!important` برای اطمینان از override شدن
   - تنظیم `visibility: hidden` و `z-index: 0`

### کد تغییر یافته:

```javascript
// Force hide language screen first
const langScreen = document.getElementById('language-screen');
if (langScreen) {
    langScreen.classList.remove('active');
    langScreen.style.display = 'none';
    langScreen.style.zIndex = '0';
}
```

```css
#language-screen:not(.active) {
    display: none !important;
    visibility: hidden !important;
    z-index: 0 !important;
}
```

## نتیجه:

✅ صفحه زبان کاملاً hide می‌شود وقتی زبان انتخاب می‌شود
✅ صفحه اصلی بلافاصله نمایش داده می‌شود
✅ نیازی به اسکرول نیست
✅ هیچ overlay یا باقی‌مانده‌ای از صفحه زبان نیست

## تست کنید:

1. صفحه را refresh کنید
2. زبان را انتخاب کنید
3. صفحه زبان باید کاملاً ناپدید شود
4. صفحه اصلی باید بلافاصله نمایش داده شود
5. نیازی به اسکرول نیست

## Deploy:

✅ Pages: Deploy شد
✅ Worker: به‌روزرسانی شد

**حالا باید کار کند!** 🎉




